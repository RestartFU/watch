CLONE(github.com/go-training/helloworld@master) AS(stable)
SET(base_path=./../../bin)
SET(binary_name=sample_binary)

RUN(cd $[stable] && go build -o $[binary_name] main.go)
EXTRACT($[stable]/$[binary_name] $[base_path])
RUN($[base_path]/$[binary_name])
